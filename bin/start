#!/bin/sh

set -e

docker run \
  --rm \
  -v $(pwd):/usr/local/docker/app  \
  hubot-mizubo \
  npm run build

docker run \
  --name hubot-mizubo \
  -i \
  -t \
  -d \
  --rm \
  --env-file .env \
  -v $(pwd):/usr/local/docker/app \
  hubot-mizubo
