mizubo:
  build: ./
  links:
    - redis
  ports:
    - 8080:8080
  environment:
    TZ: Asia/Tokyo
    REDIS_URL: redis://redis:6379
  restart: always

redis:
  restart: always
  image: redis:latest
  command: redis-server --appendonly yes
  ports:
    - 6379:6379
