{
  "name": "hubot-mizbo",
  "private": true,
  "version": "1.2.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.6.0"
  },
  "scripts": {
    "start": "lerna exec 'npm start' --scope core",
    "dev": "lerna exec 'npm run dev' --scope core",
    "build": "echo 'undefined build command'",
    "bump": "lerna version --conventional-commits --no-git-tag-version"
  }
}
